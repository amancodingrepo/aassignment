# Required for the agent loop (Phases 3+). Everything else runs without it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PARCELPILOT_MODEL=claude-sonnet-5
PARCELPILOT_DB=/data/parcelpilot.db
SESSION_SECRET=dev-only-not-a-real-secret
